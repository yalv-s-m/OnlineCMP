docker build -t app .
docker run -it app

Флаг -it устаналивает интерактивный режим, при использовании которого
программа в контейнере ставится на паузу, и ожидает ввода input.

Нужно прикрутить к сайту аналог терминала, чтобы при необходимости передать программе какие-то аргументы на вход.
